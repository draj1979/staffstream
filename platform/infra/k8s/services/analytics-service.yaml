apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: staffstream
  labels:
    app: analytics-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      containers:
        - name: analytics-service
          image: staffstream/analytics-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8009
          envFrom:
            - configMapRef: {name: staffstream-config}
            - secretRef: {name: staffstream-secrets}
          livenessProbe:
            httpGet: {path: /healthz, port: 8009}
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet: {path: /readyz, port: 8009}
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests: {cpu: "50m", memory: "128Mi"}
            limits: {cpu: "500m", memory: "256Mi"}
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: staffstream
spec:
  selector:
    app: analytics-service
  ports:
    - port: 8009
      targetPort: 8009
