apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  namespace: staffstream
  labels:
    app: audit-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit-service
  template:
    metadata:
      labels:
        app: audit-service
    spec:
      containers:
        - name: audit-service
          image: staffstream/audit-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8011
          envFrom:
            - configMapRef: {name: staffstream-config}
            - secretRef: {name: staffstream-secrets}
          livenessProbe:
            httpGet: {path: /healthz, port: 8011}
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet: {path: /readyz, port: 8011}
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests: {cpu: "50m", memory: "128Mi"}
            limits: {cpu: "500m", memory: "256Mi"}
---
apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: staffstream
spec:
  selector:
    app: audit-service
  ports:
    - port: 8011
      targetPort: 8011
