# Infra

Deployment infrastructure (Kubernetes manifests, Helm charts, Terraform).
Empty until Phase 6 (containerize, deploy to single k8s namespace).

Local dev infra (Postgres, Redis) lives in the root [docker-compose.yml](../docker-compose.yml).
